# fit-engine

Pure functions only. Zero imports from `app/`, `components/`, or `lib/supabase/` — inputs and outputs are plain domain types from `@/types/domain`. 100% test coverage required (AGENTS.md). Specification: `docs/FIT_ALGORITHM.md`; every constant lives in one constants file so recalibration is a single reviewable diff.
