import { describe, expect, it } from "vitest";

// Placeholder until the fit engine lands; proves the Vitest pipeline works.
describe("vitest setup", () => {
  it("runs a test", () => {
    expect(1 + 1).toBe(2);
  });
});
