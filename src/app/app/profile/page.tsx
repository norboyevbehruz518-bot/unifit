import { Card } from "@/components/ui/Card";

export default function ProfilePage() {
  return (
    <Card title="Your profile">
      <p className="text-body text-stone-700">
        Your profile is empty — let&apos;s fix that. We&apos;ll ask about your
        academics, budget, and a few things about you so we can find schools
        that actually fit.
      </p>
    </Card>
  );
}
